# Create your models here. contains classes and fields to be used which is modules library

#more than one entity is created hear

meta class - is a child class that controlls the behaviour of prent class

works the same as the serializer

views is used to handle HTTP request

classBased view-stuListviews
functional views-define py functionan dit will handle HTTP request

/students/register/

Cross-Site Request Forgery (CSRF) hacking a webpage unique token validation is indeed valid

### template tags

{%csrf_token%}
{{form.as_p}}



django static files


redirect foolowed by new url to the differnet page